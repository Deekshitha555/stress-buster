/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.aceternity.com','aceternity.com',"images.unsplash.com"], // Add the allowed domains here
    },
  };
  
 export default nextConfig;
  